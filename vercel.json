{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "npm install && cd packages/shared && npm install && cd ../server && npm install",
  "buildCommand": "cd packages/shared && npx tsc && cd ../server && npx tsc",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
